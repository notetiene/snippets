# -*- mode: snippet -*-
#name : Author
# key: author
# --
/**
 * Author: `(user-full-name)` (`(if(boundp 'user-mail-adress) (print user-mail-adress) (concat user-real-login-name "@" system-name))`)
 * Created: `(format-time-string "%Y-%m-%d")`
 * 
 * $0
 */